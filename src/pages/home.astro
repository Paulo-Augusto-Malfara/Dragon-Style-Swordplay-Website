---
// Mesma home da raiz. As duas rotas existem porque o menu e o dock apontam pra
// /home, e a raiz é o endereço que quem chega de fora digita.
import HomeContent from "../components/HomeContent.astro";

export const prerender = false;
---

<HomeContent />
